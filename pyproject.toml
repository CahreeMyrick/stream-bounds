[project]
name = "stream-bounds"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["numpy>=1.24"]

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"

[tool.ruff]
line-length = 100
select = ["E","F","I"]

[tool.black]
line-length = 100

